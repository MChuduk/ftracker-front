const AccentHorizontalLine = ({ spacing }) => {
  return (
    <div
      style={{
        backgroundColor: "#d8dee4",
        height: 1,
        margin: `${spacing} 0 ${spacing} 0`,
        width: "100%"
      }}
    />
  );
};

export { AccentHorizontalLine };
